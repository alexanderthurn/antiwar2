import { publicUrl } from '../core/PublicPath';

/** v1 menu pointer at 50% scale — hotspot at index fingertip (41×49). */
export const MENU_POINTER_CURSOR = `url(${publicUrl('assets/gfx/hand-cursor.png')}) 9 7, pointer`;